# SMART RT 07 RW 11 GPA NGIJO - PRODUCTION ENVIRONMENT CONFIGURATION TEMPLATE
# Do NOT place actual secret values or credentials in this file.
# Secrets must be stored exclusively in Server Environment Variables or Cloud Secrets.

# Client-side configuration (Public)
VITE_APP_NAME="SMART RT 07 RW 11 GPA NGIJO"
VITE_APP_ENV="production"
VITE_GAS_WEBAPP_URL="https://script.google.com/macros/s/AKfycbz_SMART_RT07_GPA_PROD/exec"
VITE_APP_VERSION="1.4.0"

# Server-side secrets (NEVER expose to client browser or add VITE_ prefix!)
GEMINI_API_KEY=
WHATSAPP_API_TOKEN=
WHATSAPP_WEBHOOK_SECRET=
GAS_WEBAPP_URL=
GAS_SHARED_SECRET=
AI_MODEL="gemini-2.5-flash-rt"
AI_KILL_SWITCH="ACTIVE"
APP_ENV="production"
NODE_ENV="production"
